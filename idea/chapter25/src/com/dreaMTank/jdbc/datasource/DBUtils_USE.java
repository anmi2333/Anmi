package com.dreaMTank.jdbc.datasource;

import com.dreaMTank.jdbc.utils.JDBCUtils;

/**
 * @ author WANG CHAO
 * @ version 1.0
 */
public class DBUtils_USE {//使用apache DBUTils工具类 + druid 完成对表对crud操作
    public void testQueryMany() {//返回结果是多行对情况
        //1 得到连接(druid)
        //Connection connection = JDBCUtilsBtilsByDruid.getConnection();
        //2 使用dbutils
        //3 创建对一个queryRunner
    }
}
