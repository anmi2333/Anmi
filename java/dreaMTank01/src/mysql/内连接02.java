package mysql;

public class 内连接02 {
    /*
    select * form classes cross join student; 交叉连接 4X9=36条记录
    select * form classes, student;
     select * form classes c INNER JOIN student s ON c.cid = s.cno;内连接
     select * form classes c student s WHERE c.cid = s.cno;内连接 两个表交集
    */
}
