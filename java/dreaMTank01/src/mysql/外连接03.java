package mysql;

public class 外连接03 {
     /*
    select * form classes c LEFT OUTER JOIN student s ON c.did = s.cno;左外连接
    select * form classes c RIGHT OUTER JOIN student s ON c.did = s.cno;右外连接

    */
}
